
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read and write access for all documents
    // for development purposes.
    // WARNING: Do not use these rules in production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
